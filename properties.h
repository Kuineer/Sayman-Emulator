#ifndef properties
#define properties

#define instruction_count (1 << word_size)
#define instruction_length 12
#define register_count (1 << word_size)
#define word_size 4

int clock_period_ms = 1;

#endif
